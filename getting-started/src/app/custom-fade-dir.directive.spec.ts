import { CustomFadeDirDirective } from './custom-fade-dir.directive';

describe('CustomFadeDirDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomFadeDirDirective();
    expect(directive).toBeTruthy();
  });
});
